import Link from "next/link";

type Props = {};

const Navbar = (props: Props) => {
  return (
    <header>
      <nav>
        <Link href="/">
          <img src="" alt="" />
        </Link>
      </nav>
    </header>
  );
};

export default Navbar;
